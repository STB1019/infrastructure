version: 1
metadata:
  name: Provider authorization flow (implicit consent)
entries:
- attrs:
    designation: authorization
    name: Authorize Application
    title: Redirecting to %(app)s
  identifiers:
    slug: provider-authorization-implicit
  model: authentik_flows.flow
